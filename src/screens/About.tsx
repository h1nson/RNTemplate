import React from 'react';
import { Box, Center, Text } from 'native-base';

export default () => {
    return <Box flex={1}>
        <Center>
            <Text fontSize={30}>About</Text>
        </Center>
    </Box>;
};
